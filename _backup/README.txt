index-v24-strobe-original.html
  = 튜너 스트로보 '속도에 따른 밝기' 연출을 넣기 전 상태 (v24).

되돌리려면 이 파일을 index.html 자리에 덮어쓰고,
service-worker.js의 VERSION만 지금보다 큰 값으로 올리면 된다.

이 _backup 폴더는 깃헙에 올릴 필요 없다.

index-v28-badminton.html
  = 오브 안에 배드민턴 낙서가 들어간 버전 (v28).
    올리브로 바꾼 뒤 되돌리고 싶으면 이 파일을 쓰면 된다.

index-v29-strobe-rings.html
  = 튜너가 굵은 파선 링 두 겹이던 시절 (v29). 쳇바퀴로 바꾸기 직전.
